buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Проверь, чтобы было именно двоеточие перед 8.2.2
        classpath "com.android.tools.build:gradle:8.2.2"
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
