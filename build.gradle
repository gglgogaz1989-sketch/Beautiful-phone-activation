// Top-level build file
buildscript {
    dependencies {
        // Эта строка теперь будет искать плагин через настройки в settings.gradle
        classpath "com.android.tools.build:gradle:8.2.2"
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
