pluginManagement {
    repositories {
        google()        // КРИТИЧНО: Чтобы найти com.android.tools.build:gradle
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()        // Для библиотек (AppCompat, Material и т.д.)
        mavenCentral()
    }
}
rootProject.name = "StrangeBoot"
include ':app'
